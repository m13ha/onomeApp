{
  "name": "onomeapi",
  "version": "1.0.0",
  "description": "The backend for onome student application",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "client": "cd client && npm run start",
    "dev": "concurrently \"nodemon server.js --ignore client\" \"npm run client\""
  },
  "author": "Angelmikeal",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "concurrently": "^7.0.0",
    "dotenv": "^11.0.0",
    "express": "^4.17.2",
    "mongoose": "^6.1.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
